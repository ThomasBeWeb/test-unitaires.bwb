{% extends 'base.html.twig' %}

{% block body %}
<div class="container">

    <div class="row">
        <div class="col-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
    <div class="row">
        <div class="col-4">
            <h5>Prix du billet</h5><br>
            <h5 id="prixBillet">{{ price }} €</h5>
        </div>
    </div>

</div>


{% endblock %}
